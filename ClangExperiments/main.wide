using Optional := Standard.Container.Optional;
using None := Standard.Container.None;
using Map := Standard.Algorithm.Map;
Main() {
    r := function()[min := 0, max := 10] {
        if (min == max) return Optional!(int8)(None);
        var := Optional!(int8)(min);
        min += 1;
        return var;
    };
    Standard.Algorithm.Map(
}