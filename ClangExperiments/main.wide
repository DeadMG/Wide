using std := cpp("WideLibrary/test.h").std;
sort(cont, func) {
    cont.push_back(66);
    cont.push_back(65);
    std.sort(cont.begin(), cont.end(), func);
    std.cout << cont.front() << cont.back(); 
}
sort(cont) {
    return sort(cont, less);
}
less(int8 lhs, int8 rhs) {
    return true;
}
Main() {
    vec := std.vector!(int8)();
    deq := std.deque!(int8)();
    sort(vec, less);
    sort(deq, less);
}