using test := cpp("WideLibrary/test.h");
using Range := Standard.Range;
using Algorithm := Standard.Algorithm;

Main() {
    Standard.IO.Stream("CONOUT$")(uint8(uint64(66)));
    //x := std.string("Hello, World!");
    //y := x;
    //z := std.move(x);
    //std.cout << z;
}