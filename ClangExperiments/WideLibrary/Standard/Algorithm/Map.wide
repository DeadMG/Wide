module Standard {
    module Algorithm {
        Map(range, fun) {
            return function()[range := std.move(range), fun := std.move(fun)] {
                 var := range();
                 if (var) return Containers.Optional!(int8)(fun(*std.move(var)));
                 return Containers.Optional!(int8)(Containers.None);
            };
        }
    }
}