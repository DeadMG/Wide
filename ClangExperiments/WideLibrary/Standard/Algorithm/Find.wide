module Standard {
    module Algorithm {
        Find(func) {
            return function(range)[func := std.move(func)] {
                var := range();
                while (var) {
                    if (func(*var)) return std.move(var);
                    var = range();
                }
                return Containers.None;
            };
        }
    }
}