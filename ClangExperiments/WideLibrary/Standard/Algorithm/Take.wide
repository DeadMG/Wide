module Standard {
    module Algorithm {
        Take(count) {
            return function(range)[count := count] {
                return function()[range := std.move(range), begin := 0] {
                    if(begin ~= count) {
                        ++begin;
                        return range();
                    }
                    return Containers.None;
                };
            };
        }
    }
}