module Standard {
    module Algorithm {
        Drop(count) {
            return function(range)[count := count] {
                return function()[range := std.move(range), begin := 0] {
                    while(begin ~= count) {
                        range();
                        ++begin;
                    }
                    return range();
                };
            };
        }
    }
}