module Standard {
    module Algorithm {
        ForEach(fun) {
            return function(range)[fun := std.move(fun)] {
                var := range();
                while(var) {
                    fun(*std.move(var));
                    var = range();
                }
            };
        }
    }
}