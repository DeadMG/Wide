module Standard {
    module Algorithms {
        All(pred) {
            return function(range)[pred := std.move(pred)] {
                result := true;
                var := range();
                while(result & var) {
                    result = pred(*var);
                    var = range();
                }
                return result;
            };
        }
    }
}