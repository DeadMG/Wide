module DeferredLambda {
    f(x) {
        if (x > 5) {
            z := f(3) + 1;
            return function() { return z; }();
        }
        return x;
    }
    Main() {
        int64(f(6));
    }
}