// Fails if mixing C++ and Wide overload sets fails or resolves to the wrong overload.
using clangtest := cpp("JitSuccess/ClangMixedADL.h").ClangADL.test;
module Test {
    type t {}
    operator==(t lhs, clangtest rhs) { return true; }
}
Main() {
    return Test.t() == clangtest();
}