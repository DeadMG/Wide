Main() {
    x := type { 
        pointer := bool.pointer;
        type(p := bool.lvalue) : pointer(&p) {}
        type(other := this.lvalue) : pointer(other.pointer) {
            *pointer = true;
        }
    };
    result := false;
    first := x(result);
    copy := first;
    return result;
}