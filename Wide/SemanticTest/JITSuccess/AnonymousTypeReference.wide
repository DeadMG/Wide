Main() {
    x := type { 
        pointer := bool->pointer;
        type(bool->lvalue p) : pointer(&p) {}
        type(this->lvalue other) : pointer(other.pointer) {
            *pointer = true;
        }
    };
    result := false;
    first := x(result);
    copy := first;
    return result;
}