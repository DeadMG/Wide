using header := cpp("JITSuccess/CPPInterop/InheritCPPInterface.h");
using test := header.test;
type t : test {
    f() { return true; }
}
Main() {
    var := t();
    ref := header.test.lvalue(var);
    return header.g(ref);
}