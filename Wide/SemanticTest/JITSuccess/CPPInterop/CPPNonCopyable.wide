using movable := cpp("JITSuccess/CPPInterop/CPPNonCopyable.h").test;
f(movable x) {
    return false;
}
f(auto->lvalue x) {
    return true;
}
Main() {
    var := movable();
    return f(var);
}