using header := cpp("JITSuccess/CPPInterop/ExportEmptyMemberFunction.h");
f(header.test.pointer self) prolog {
    ExportName = cpp.mangle(header.test().f);
} {
    return true;
}
Main() {
    f(null);
    return header.test().g();
}