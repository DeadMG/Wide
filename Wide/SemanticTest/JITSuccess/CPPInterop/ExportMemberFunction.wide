using header := cpp("JITSuccess/CPPInterop/ExportMemberFunction.h");
f(header.test->pointer self) prolog {
    ExportName = cpp.mangle(header.test().f);
} {
    if (self)
        self->var = true;
}
Main() {
    f(null);
    var := header.test();
    var.g();
    return var.var;
}