using header := cpp("JITSuccess/CPPInterop/ExportMemberFunction.h");
f(self := header.test.pointer) prolog {
    ExportAs = header.test().f;
} {
    if (self)
        self->var = true;
}
Main() {
    var := header.test();
    var.g();
    return var.var;
}