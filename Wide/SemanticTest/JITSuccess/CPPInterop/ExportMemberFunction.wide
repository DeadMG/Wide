using header := cpp("JITSuccess/CPPInterop/ExportMemberFunction.h");

[export := header.test().f]
f(self := header.test.pointer) {
    if (self)
        self->var = true;
}
Main() {
    var := header.test();
    var.g();
    return var.var;
}