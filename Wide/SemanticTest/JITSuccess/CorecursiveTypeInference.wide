// Fails if type inference fails to infer the type of the function.
foo(x) {
    if (x > 5)
        return bar(x);
    return x;
}
bar(x) {
    return foo(x - 1);
}
Main() {
    return foo(6) == 5;
}