// Tests that booleans short-circuit evaluate correctly.
f(bool->lvalue arg) {
    arg = false;
    return true;
}
Main() {
    ret := true;
    true | f(ret);
    false & f(ret);
    return ret;
}