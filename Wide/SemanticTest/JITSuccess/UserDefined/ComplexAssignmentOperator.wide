type t {
    val := bool;
    type() : val(false) {}
    type(other := this.rvalue) : val(other.val) {}
    operator=(other := this.lvalue) { val = true; }
}
Main() {
    x, y := { t(), t() };
    x = y;
    return x.val;
}