using cplusplus := cpp.literal("#include <string>
struct base { virtual std::string g() = 0; };
std::string f(base&& arg) { return arg.g(); }");
type t : cplusplus.base {
    g() { return "Hello, World!"; }
}
Main() {
    std.cout << cplusplus.f(t()).c_str();
    return true;
}