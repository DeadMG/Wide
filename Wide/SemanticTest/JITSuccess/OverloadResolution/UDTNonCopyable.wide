type movable {
    type(arg := movable.rvalue) {}
    type() {}
}
f(x := movable) {
    return false;
}
f(x := auto.lvalue) {
    return true;
}
Main() {
    var := movable();
    return f(var);
}