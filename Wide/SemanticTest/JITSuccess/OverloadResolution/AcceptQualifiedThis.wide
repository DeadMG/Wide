// Fails if qualified this lookup finds the wrong overload or fails to analyze.
type t1 {
    operator()(t1.lvalue this) {
        return true;
    }
    operator()(t1.rvalue this) {
        return false;
    }
}
type t2 {
    operator()(t2.rvalue this) {
        return true;
    }
    operator()(t2.lvalue this) {
        return false;
    }
}
Main() {
    x := t1();
    return x() & t2()();
}