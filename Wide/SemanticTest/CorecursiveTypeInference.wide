module CoRecursiveTypeInference {
    foo(x) {
        if (x > 5)
            return bar(x);
        return x;
    }
    bar(x) {
        return foo(x - 1);
    }
    Main() {
        int64(foo(6));
    }
}