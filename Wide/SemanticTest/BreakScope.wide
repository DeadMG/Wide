type testty {
    result := bool->pointer;
    ~type() {
         *result = false;
    }
    f() {
        return true;
    }
    type(bool->pointer arg)
        : result(arg) {}
}
Main() {
    result := true;
    var := testty(&result);
    while(true)
        break;
    return result;
}